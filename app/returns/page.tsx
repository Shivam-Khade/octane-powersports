export const metadata = {
  title: "Returns Policy"
};

export default function ReturnsPage() {
  return (
    <main className="container" style={{ padding: '120px 20px', minHeight: '60vh' }}>
      <h1>Returns Policy</h1>
      <p style={{ marginTop: '20px', maxWidth: '800px', lineHeight: '1.6' }}>
        We offer a 15-day return policy for all unused and unmounted products in original packaging. 
        Safety equipment like helmets must have tags attached and visors unpeeled.
      </p>
    </main>
  );
}
