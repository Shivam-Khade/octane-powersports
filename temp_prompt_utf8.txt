<USER_REQUEST>
You are rebuilding the UI/UX of Octane Powersports (octane-powersports.vercel.app), 
a premium Indian motorcycle gear and performance parts e-commerce store. 
The existing codebase is Next.js. Preserve all routing, data, and functionality. 
Implement the following UI/UX improvements while maintaining the brand theme: 
white background (#FFFFFF page, #F8F8F7 body), black typography (#111111 primary, 
#555555 secondary), and orange accent (#F97316 for CTAs, #D4620A for text-use orange 
that passes WCAG AA contrast).

---

CRITICAL BUGS TO FIX FIRST:

1. In the cart sidebar, prices display in USD ($1,199). Fix to use INR (Γé╣) consistently 
   across the entire application. Find all instances of dollar sign or usd formatting 
   and replace with Indian Rupee formatting using Intl.NumberFormat('en-IN', 
   {style: 'currency', currency: 'INR', maximumFractionDigits: 0}).

2. In the navigation mega-menu, there is a "Rider Edits" section with placeholder text 
   "curated by superbike brand." ΓÇö find and replace with real copy: 
   "Race-ready kits curated by our expert riders."

3. In the footer and support links, multiple href="#" placeholder links exist for 
   Returns Policy, Shipping Info, FAQs, Fitment Help, About Us. Create minimal 
   placeholder pages at /returns, /shipping, /faq, /about with real policy content 
   stubs so links are not dead.

---

TYPOGRAPHY SYSTEM:

Import the following fonts from Google Fonts:
- Display font: "Barlow Condensed" weights 600, 700 ΓÇö for hero headlines, 
  section titles, and product names
- Body font: "DM Sans" weights 400, 500 ΓÇö for all body text, navigation, 
  and UI elements

Create a CSS custom property system:
  --font-display: 'Barlow Condensed', sans-serif;
  --font-body: 'DM Sans', sans-serif;
  --text-hero: clamp(64px, 8vw, 100px);
  --text-section: clamp(36px, 4vw, 52px);
  --text-card-title: 18px;
  --text-body: 16px;
  --text-small: 14px;
  --text-micro: 12px;
  --tracking-display: 0.04em;
  --tracking-caps: 0.12em;

Apply --font-display to al
<truncated 9624 bytes>
tom bar 
   with 4 icons: Home, Shop, Search, Cart (with badge count).
   White background, border-top 1px solid #E5E5E5.
   This is standard for all premium mobile e-commerce.

4. Express checkout: On the product page, below the Add to Cart button, 
   add express checkout options:
   "Buy with UPI" button (outlined, with UPI logo)
   "Pay in EMI" button (outlined)

---

ACCESSIBILITY:

1. Add a skip-to-main-content link as the very first element in the body:
   <a href="#main-content" class="skip-link">Skip to main content</a>
   Visually hidden until focused:
   .skip-link { position: absolute; top: -40px; }
   .skip-link:focus { top: 0; background: #F97316; color: white; 
   padding: 8px 16px; z-index: 10000; }

2. All interactive elements must have :focus-visible styles:
   outline: 2px solid #F97316; outline-offset: 2px;

3. All icon-only buttons must have aria-label attributes.

4. Color check: Any orange text smaller than 18px must use #C55A00 
   (not #F97316) to pass WCAG AA 4.5:1 contrast ratio against white.

---

Keep all existing Next.js routing, data fetching, Typescript types, 
and component logic intact. Only modify styling and add new UI components. 
Do not modify any API calls, product data structures, or checkout flow logic.

After implementing all changes, run a visual diff pass and confirm:
  1. No placeholder text remains anywhere visible
  2. All prices display in Γé╣ (INR) format
  3. All links resolve to real pages or are removed
  4. Orange accent is used only for primary CTAs and highlights 
     (not for decorative elements)
  5. All font usages have correct fallbacks
</USER_REQUEST>
<ADDITIONAL_METADATA>
The current local time is: 2026-06-03T12:33:18+05:30.
</ADDITIONAL_METADATA>
<USER_SETTINGS_CHANGE>
The user changed setting `Model Selection` from Gemini 3.5 Flash (Medium) to Gemini 3.1 Pro (High). No need to comment on this change if the user doesn't ask about it. If reporting what model you are, please use a human readable name instead of the exact string.
</USER_SETTINGS_CHANGE>
